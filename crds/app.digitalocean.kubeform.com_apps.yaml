apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: digitalocean.kubeform.com
    app.kubernetes.io/part-of: kubeform.com
  name: apps.app.digitalocean.kubeform.com
spec:
  group: app.digitalocean.kubeform.com
  names:
    kind: App
    listKind: AppList
    plural: apps
    singular: app
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              kubeformOutput:
                properties:
                  activeDeploymentID:
                    description: The ID the App's currently active deployment
                    type: string
                  createdAt:
                    description: The date and time of when the App was created
                    type: string
                  defaultIngress:
                    description: The default URL to access the App
                    type: string
                  id:
                    type: string
                  liveURL:
                    type: string
                  spec:
                    description: A DigitalOcean App Platform Spec
                    properties:
                      database:
                        items:
                          properties:
                            clusterName:
                              description: The name of the underlying DigitalOcean
                                DBaaS cluster. This is required for production databases.
                                For dev databases, if cluster_name is not set, a new
                                cluster will be provisioned.
                              type: string
                            dbName:
                              description: The name of the MySQL or PostgreSQL database
                                to configure.
                              type: string
                            dbUser:
                              description: The name of the MySQL or PostgreSQL user
                                to configure.
                              type: string
                            engine:
                              description: The database engine to use.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            production:
                              description: Whether this is a production or dev database.
                              type: boolean
                            version:
                              description: The version of the database engine.
                              type: string
                          type: object
                        type: array
                      domain:
                        items:
                          properties:
                            name:
                              description: The hostname for the domain.
                              type: string
                            type:
                              description: The type of the domain.
                              type: string
                            wildcard:
                              description: Indicates whether the domain includes all
                                sub-domains, in addition to the given domain.
                              type: boolean
                            zone:
                              description: If the domain uses DigitalOcean DNS and
                                you would like App Platform to automatically manage
                                it for you, set this to the name of the domain on
                                your account.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      domains:
                        description: Deprecated
                        items:
                          type: string
                        type: array
                      env:
                        items:
                          properties:
                            key:
                              description: The name of the environment variable.
                              type: string
                            scope:
                              description: The visibility scope of the environment
                                variable.
                              type: string
                            type:
                              description: The type of the environment variable.
                              type: string
                          type: object
                        type: array
                      job:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            kind:
                              description: The type of job and when it will be run
                                during the deployment process.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      name:
                        description: The name of the app. Must be unique across all
                          apps in the same account.
                        type: string
                      region:
                        description: The slug for the DigitalOcean data center region
                          hosting the app
                        type: string
                      service:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            healthCheck:
                              properties:
                                failureThreshold:
                                  description: The number of failed health checks
                                    before considered unhealthy.
                                  format: int64
                                  type: integer
                                httpPath:
                                  description: The route path used for the HTTP health
                                    check ping.
                                  type: string
                                initialDelaySeconds:
                                  description: The number of seconds to wait before
                                    beginning health checks.
                                  format: int64
                                  type: integer
                                periodSeconds:
                                  description: The number of seconds to wait between
                                    health checks.
                                  format: int64
                                  type: integer
                                successThreshold:
                                  description: The number of successful health checks
                                    before considered healthy.
                                  format: int64
                                  type: integer
                                timeoutSeconds:
                                  description: The number of seconds after which the
                                    check times out.
                                  format: int64
                                  type: integer
                              type: object
                            httpPort:
                              description: The internal port on which this service's
                                run command will listen.
                              format: int64
                              type: integer
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            internalPorts:
                              items:
                                format: int64
                                type: integer
                              type: array
                            name:
                              description: The name of the component
                              type: string
                            routes:
                              items:
                                properties:
                                  path:
                                    description: Path specifies an route by HTTP path
                                      prefix. Paths must start with / and must be
                                      unique within the app.
                                    type: string
                                type: object
                              type: array
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                      staticSite:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            catchallDocument:
                              description: The name of the document to use as the
                                fallback for any requests to documents that are not
                                found when serving this static site.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            errorDocument:
                              description: The name of the error document to use when
                                serving this static site.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            indexDocument:
                              description: The name of the index document to use when
                                serving this static site.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            outputDir:
                              description: 'An optional path to where the built assets
                                will be located, relative to the build context. If
                                not set, App Platform will automatically scan for
                                these directory names: `_static`, `dist`, `public`.'
                              type: string
                            routes:
                              items:
                                properties:
                                  path:
                                    description: Path specifies an route by HTTP path
                                      prefix. Paths must start with / and must be
                                      unique within the app.
                                    type: string
                                type: object
                              type: array
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      worker:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    required:
                    - name
                    type: object
                  updatedAt:
                    description: The date and time of when the App was last updated
                    type: string
                type: object
              providerRef:
                description: LocalObjectReference contains enough information to let
                  you locate the referenced object inside the same namespace.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              resource:
                properties:
                  activeDeploymentID:
                    description: The ID the App's currently active deployment
                    type: string
                  createdAt:
                    description: The date and time of when the App was created
                    type: string
                  defaultIngress:
                    description: The default URL to access the App
                    type: string
                  id:
                    type: string
                  liveURL:
                    type: string
                  spec:
                    description: A DigitalOcean App Platform Spec
                    properties:
                      database:
                        items:
                          properties:
                            clusterName:
                              description: The name of the underlying DigitalOcean
                                DBaaS cluster. This is required for production databases.
                                For dev databases, if cluster_name is not set, a new
                                cluster will be provisioned.
                              type: string
                            dbName:
                              description: The name of the MySQL or PostgreSQL database
                                to configure.
                              type: string
                            dbUser:
                              description: The name of the MySQL or PostgreSQL user
                                to configure.
                              type: string
                            engine:
                              description: The database engine to use.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            production:
                              description: Whether this is a production or dev database.
                              type: boolean
                            version:
                              description: The version of the database engine.
                              type: string
                          type: object
                        type: array
                      domain:
                        items:
                          properties:
                            name:
                              description: The hostname for the domain.
                              type: string
                            type:
                              description: The type of the domain.
                              type: string
                            wildcard:
                              description: Indicates whether the domain includes all
                                sub-domains, in addition to the given domain.
                              type: boolean
                            zone:
                              description: If the domain uses DigitalOcean DNS and
                                you would like App Platform to automatically manage
                                it for you, set this to the name of the domain on
                                your account.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      domains:
                        description: Deprecated
                        items:
                          type: string
                        type: array
                      env:
                        items:
                          properties:
                            key:
                              description: The name of the environment variable.
                              type: string
                            scope:
                              description: The visibility scope of the environment
                                variable.
                              type: string
                            type:
                              description: The type of the environment variable.
                              type: string
                          type: object
                        type: array
                      job:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            kind:
                              description: The type of job and when it will be run
                                during the deployment process.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      name:
                        description: The name of the app. Must be unique across all
                          apps in the same account.
                        type: string
                      region:
                        description: The slug for the DigitalOcean data center region
                          hosting the app
                        type: string
                      service:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            healthCheck:
                              properties:
                                failureThreshold:
                                  description: The number of failed health checks
                                    before considered unhealthy.
                                  format: int64
                                  type: integer
                                httpPath:
                                  description: The route path used for the HTTP health
                                    check ping.
                                  type: string
                                initialDelaySeconds:
                                  description: The number of seconds to wait before
                                    beginning health checks.
                                  format: int64
                                  type: integer
                                periodSeconds:
                                  description: The number of seconds to wait between
                                    health checks.
                                  format: int64
                                  type: integer
                                successThreshold:
                                  description: The number of successful health checks
                                    before considered healthy.
                                  format: int64
                                  type: integer
                                timeoutSeconds:
                                  description: The number of seconds after which the
                                    check times out.
                                  format: int64
                                  type: integer
                              type: object
                            httpPort:
                              description: The internal port on which this service's
                                run command will listen.
                              format: int64
                              type: integer
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            internalPorts:
                              items:
                                format: int64
                                type: integer
                              type: array
                            name:
                              description: The name of the component
                              type: string
                            routes:
                              items:
                                properties:
                                  path:
                                    description: Path specifies an route by HTTP path
                                      prefix. Paths must start with / and must be
                                      unique within the app.
                                    type: string
                                type: object
                              type: array
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                      staticSite:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            catchallDocument:
                              description: The name of the document to use as the
                                fallback for any requests to documents that are not
                                found when serving this static site.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            errorDocument:
                              description: The name of the error document to use when
                                serving this static site.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            indexDocument:
                              description: The name of the index document to use when
                                serving this static site.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            outputDir:
                              description: 'An optional path to where the built assets
                                will be located, relative to the build context. If
                                not set, App Platform will automatically scan for
                                these directory names: `_static`, `dist`, `public`.'
                              type: string
                            routes:
                              items:
                                properties:
                                  path:
                                    description: Path specifies an route by HTTP path
                                      prefix. Paths must start with / and must be
                                      unique within the app.
                                    type: string
                                type: object
                              type: array
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                      worker:
                        items:
                          properties:
                            buildCommand:
                              description: An optional build command to run while
                                building this component from source.
                              type: string
                            dockerfilePath:
                              description: The path to a Dockerfile relative to the
                                root of the repo. If set, overrides usage of buildpacks.
                              type: string
                            env:
                              items:
                                properties:
                                  key:
                                    description: The name of the environment variable.
                                    type: string
                                  scope:
                                    description: The visibility scope of the environment
                                      variable.
                                    type: string
                                  type:
                                    description: The type of the environment variable.
                                    type: string
                                type: object
                              type: array
                            environmentSlug:
                              description: An environment slug describing the type
                                of this app.
                              type: string
                            git:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                repoCloneURL:
                                  description: The clone URL of the repo.
                                  type: string
                              type: object
                            github:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            gitlab:
                              properties:
                                branch:
                                  description: The name of the branch to use.
                                  type: string
                                deployOnPush:
                                  description: Whether to automatically deploy new
                                    commits made to the repo
                                  type: boolean
                                repo:
                                  description: The name of the repo in the format
                                    `owner/repo`.
                                  type: string
                              type: object
                            image:
                              properties:
                                registry:
                                  description: The registry name. Must be left empty
                                    for the DOCR registry type.
                                  type: string
                                registryType:
                                  description: The registry type.
                                  type: string
                                repository:
                                  description: The repository name.
                                  type: string
                                tag:
                                  description: The repository tag. Defaults to latest
                                    if not provided.
                                  type: string
                              required:
                              - registryType
                              - repository
                              type: object
                            instanceCount:
                              description: The amount of instances that this component
                                should be scaled to.
                              format: int64
                              type: integer
                            instanceSizeSlug:
                              description: The instance size to use for this component.
                              type: string
                            name:
                              description: The name of the component
                              type: string
                            runCommand:
                              description: An optional run command to override the
                                component's default.
                              type: string
                            sourceDir:
                              description: An optional path to the working directory
                                to use for the build.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    required:
                    - name
                    type: object
                  updatedAt:
                    description: The date and time of when the App was last updated
                    type: string
                type: object
              secretRef:
                description: LocalObjectReference contains enough information to let
                  you locate the referenced object inside the same namespace.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              terminationPolicy:
                enum:
                - Delete
                - DoNotTerminate
                type: string
              updatePolicy:
                enum:
                - Destroy
                - DoNotDestroy
                type: string
            required:
            - providerRef
            - resource
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another. This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: If set, this represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.condition[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: The reason for the condition's last transition
                        in CamelCase. The specific API may choose whether or not this
                        field is considered a guaranteed API. This field may not be
                        empty.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Resource generation, which is updated on mutation by
                  the API Server.
                format: int64
                type: integer
              phase:
                description: Status defines the set of statuses a resource can have.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
